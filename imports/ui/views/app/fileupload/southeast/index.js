import React, { Component } from "react";
import CommonPage from '../common';

export default class SoutheastPage extends Component {
  render() {
    return (
      <CommonPage {...this.props}/>
    );
  }
}