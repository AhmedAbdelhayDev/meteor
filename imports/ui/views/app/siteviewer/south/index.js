import React, { Component } from "react";
import CommonPage from '../common';

export default class SouthPage extends Component {
  render() {
    return (
      <CommonPage {...this.props}/>
    );
  }
}